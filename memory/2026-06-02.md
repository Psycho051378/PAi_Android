# 2 июня 2026

## Что делали
- **HTTP Router Client**: TP-Link Archer прошивка возвращает XML/HTML вместо JSON на login. Логин через POST `password=***` работает (status=200), но Set-Cookie пустой, и все эндпоинты ARP (userRpm/*.htm) возвращают 404.
- **UPnP Client**: Написан UpnpRouterClient. M-SEARCH работает — находит LOCATION: `http://192.168.0.1:1900/ixmvf/rootDesc.xml`. Парсит XML, находит service URLs. Но SOAP запросы возвращают 404 на порту 80. Исправлено на порт 1900.
- **PowerShell повредил** `UpnpRouterClient.kt` — съел сигнатуру `getBaseUrl()`. Нужно переписать файл целиком.

## Проблемы открытые
1. UpnpRouterClient.kt не компилируется — разбалансировка скобок после PowerShell-правок
2. UPnP SOAP запросы на порт 1900 — нужно тестировать
3. HTTP Router Client не работает для Archer (все ARP эндпоинты 404)

## Email Client
- Добавлена кнопка "Редактировать" (Create иконка) в EmailAccountsDialog (SkillStoreScreen.kt)
- Универсальный диалог EmailAccountDialog для добавления/редактирования

## Настройки роутера
- Интегрированы в HomeSkill через SharedPreferences (RouterConfigData)
- Доступны через Skill Store → Home Network → "Настройки"
- RouterScanner больше не зависит от Room DAO
- Удалены RouterCredential entity и DAO (данные через SharedPreferences)
- **Версия БД откатилась с 18 на 17 → destructive migration удалила все чаты!**

## Окно любопытства
- Не использовано сегодня
